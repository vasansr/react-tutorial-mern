var BugList = React.createClass({
  render: function() {
    return (
      <div>The bug list would come here.</div>
    )
  }
});

ReactDOM.render(
  <BugList />,
  document.getElementById('main')
);

